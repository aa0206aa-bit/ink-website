# 序序・流動墨網站 設計文件

日期：2026-06-12（2026-07-02 視覺方向修訂：墨夜畫廊 → 和紙晝色）
狀態：已與決策者逐節確認，待最終審閱

## 1. 專案概述

「序序」是販售藝術流動墨（alcohol ink 類）的品牌。本網站是品牌門面：以**可互動的墨水流體體驗**讓訪客親手感受墨水技法，再以作品與產品說服購買，購買行為外連至既有賣場。

- 受眾：對流動墨藝術有興趣的消費者（手作、藝術愛好者）
- 商業模式：展示型網站＋外連購買（無站內金流）
- 語言：繁體中文

## 2. 已確認決策記錄

| 決策點 | 結論 |
|--------|------|
| 產品類型 | 藝術流動墨（alcohol ink 等） |
| 銷售功能 | 展示型＋外連購買，連結先佔位（`#`） |
| 品牌名 | 序序 |
| 作品照片 | 先用程式生成的墨水風佔位視覺，日後換實照 |
| 互動形式 | 技法工具列＋主畫布（方案 C） |
| 視覺風格 | ~~墨夜畫廊~~ → **和紙晝色**（2026-07-02 修訂：米色和紙底＋減法混色墨韻，參考墨流し／Suminagashi 做法） |
| 畫布引擎 | Three.js ＋ WebGL shader 流體模擬（stable fluids） |
| 檔案組織 | 3 檔：`index.html` / `fluid.js` / `three.min.js`（本地） |
| 墨色呈現 | 減法混色：dye 存 absorption（吸收）向量，顯示 `paperColor * exp(-absorption)`（Beer-Lambert），墨「沉入紙裡」 |
| 畫布氛圍 | 閒置自動演出＋載入初始滴墨＋清除為 1–2 秒漸淡（洗い流す） |

## 3. 頁面結構（單頁，由上而下）

1. **導覽列** — 「序序」字標＋錨點連結（體驗・墨色・作品・關於）
2. **Hero** — 品牌名＋標語＋「往下捲親手玩墨」引導。背景為 CSS 靜態墨暈（radial-gradient＋微緩動畫），**不放第二個 WebGL context**，互動聚焦於體驗區
3. **互動體驗區（核心）** — 技法工具列＋主畫布＋色盤＋清除鍵
4. **墨色（產品區）** — 每色一卡：色票、色號名、「帶它回家」外連按鈕；與色盤同一份資料來源
5. **作品展示** — 不規則磚牆排列（CSS grid），佔位視覺先頂位
6. **關於序序** — 三五句品牌故事（佔位文案）
7. **Footer** — IG／蝦皮／聯絡（佔位連結）＋版權

## 4. 互動體驗區設計（核心功能）

### 4.1 三種技法

| 技法 | 操作（桌機／手機通用） | 流體輸入 |
|------|------------------------|----------|
| 滴墨 | 點擊／輕觸畫布 | 注入染料＋微弱徑向速度脈衝 → 自然暈開 |
| 吹墨 | 按住拖曳 | 沿軌跡注入強方向速度、窄半徑 → 觸鬚狀紋路 |
| 傾斜 | 拖曳方向＝傾斜方向 | 拖曳向量轉為全域外力，整片墨緩慢流動，鬆手漸停 |

### 4.2 流體引擎（`fluid.js`）

- 標準 stable fluids 演算法：advection（平流）→ divergence（散度）→ pressure solve（壓力求解，Jacobi 迭代）→ gradient subtract（梯度修正），染料場獨立 advect
- Three.js 職責：WebGL context、`WebGLRenderTarget` ping-pong（雙緩衝）、`ShaderMaterial` 管理、全螢幕 quad 渲染
- 對外介面（頁面只透過這層溝通，不碰內部）：
  - `new InkSimulation(canvasElement, options)`
  - `setColor(hex)` — 設定當前墨色
  - `setMode('drop' | 'blow' | 'tilt')` — 切換技法
  - `clear()` — 洗い流す式漸淡清空（約 1.5 秒墨漸被水洗走，非瞬間消失；2026-07-02 修訂）
  - `splatAt(u, v, hex?)` — 程式化滴墨（0–1 畫布座標；自動演出與初始動畫用；2026-07-02 新增）
  - `pause()` / `resume()` — 暫停／恢復模擬
  - 指標事件由引擎內部監聽 canvas 處理

### 4.4 畫布氛圍（2026-07-02 新增，參考墨流し）

- **載入初始滴墨**：頁面載入後自動錯時滴三滴（不同色、不同位置），首屏即有畫面
- **閒置自動演出**：使用者閒置約 6 秒後，隨機位置自動滴墨、偶爾補第二滴鄰近異色、持續輕微水流；使用者一互動即停，重新計時
- `prefers-reduced-motion: reduce` 時：自動演出頻率減半、水流減弱

### 4.3 色盤連動產品

- 色盤色球＝販售色號，點選作畫色即切換
- 色球與產品卡渲染自同一份 `INKS` 資料（見 §6.2）

## 5. 視覺設計（和紙晝色；2026-07-02 修訂，原墨夜畫廊）

### 5.1 色彩系統

| 用途 | 色值 |
|------|------|
| 頁面背景（和紙米色） | `#efeae0` |
| 面板／卡片底 | `#f7f3ea`（略亮米白） |
| 主文字 | `#2a2723`（墨黑） |
| 次要文字 | `#2a2723` 降透明度 |
| 點綴／按鈕框線 | `#8a6d1f`（深金褐；原金 `#d4af37` 在米色底對比不足） |

### 5.1.1 畫布渲染（減法混色＋紙質感）

- dye field 儲存 **absorption 向量**（splat 時 `-log(clamp(inkColor, ε, 1))` 轉換），多滴墨疊加＝吸收相加＝顏色更深，物理正確
- display shader：`paperColor * exp(-absorption)`，紙底色與頁面背景同為 `#efeae0`
- 和紙纖維：高、中、低三頻 noise 疊加微擾紙面，避免平滑塑膠感
- 邊緣加很淡的 vignette（暗角）
- `setColor(hex)` 對外介面不變，轉換在引擎內部

### 5.2 佔位色號（日後可換可增；2026-07-02 調深適配吸收模型，色號名沿用）

| 色號 | 名稱 | 色值 |
|------|------|------|
| №01 | 初潮 | `#0f6b63`（深青綠） |
| №02 | 夜枝 | `#8f2050`（酒紅） |
| №03 | 鎏金 | `#9a7b1e`（土金） |
| №04 | 靜淵 | `#24418f`（藏青） |

### 5.3 字體

- 標題：Google Fonts「Noto Serif TC」（CDN 載入，`font-display: swap`），fallback `"Songti TC", Georgia, serif`。字型 CDN 失效時退回系統字型，不影響功能，故接受此一 CDN 依賴
- 內文：系統無襯線堆疊 `-apple-system, "PingFang TC", "Microsoft JhengHei", sans-serif`
- 品牌字「序序」：寬字距（letter-spacing）襯線呈現

## 6. 技術架構

### 6.1 檔案組織

```
~/projects/ink-website/
├── index.html      # 頁面結構＋樣式＋頁面邏輯（內嵌 CSS/JS）
├── fluid.js        # 流體引擎模組（GLSL shader＋InkSimulation 類別）
├── three.min.js    # Three.js 本地檔（不依賴 CDN；版本於實作時下載並記錄）
└── docs/superpowers/specs/  # 本文件
```

- 純靜態，無 build 工具、無框架；部署可用任何靜態空間（本期不含部署）
- 實作 Three.js 相關程式前，先以 Context7 查詢對應版本最新 API 用法

### 6.2 產品資料（單一事實來源）

```js
const INKS = [
  { no: '01', name: '初潮', hex: '#0f6b63', buyUrl: '#' },
  { no: '02', name: '夜枝', hex: '#8f2050', buyUrl: '#' },
  { no: '03', name: '鎏金', hex: '#9a7b1e', buyUrl: '#' },
  { no: '04', name: '靜淵', hex: '#24418f', buyUrl: '#' },
];
```

色盤、產品卡皆由此渲染。日後改連結、改名、增色，只改這一份。

## 7. 效能策略

- 模擬解析度與染料解析度分離：速度場 128–256 格；染料桌機 1024／手機 512
- `devicePixelRatio` 上限 2
- IntersectionObserver：畫布捲出視口 → `pause()`；`visibilitychange` 切背景分頁 → `pause()`
- `prefers-reduced-motion`：停用 hero 自動動畫；互動畫布保留（屬用戶主動操作），閒置自動演出頻率減半、水流減弱（§4.4）

## 8. 錯誤處理

| 情境 | 處理 |
|------|------|
| WebGL 不可用 | 體驗區顯示靜態墨暈視覺＋文案提示，其餘區塊正常。不寫 Canvas 2D 備援引擎（工程量翻倍，>97% 瀏覽器支援 WebGL，不值得） |
| WebGL context lost（手機切 app 常見） | 監聽 `webglcontextlost` / `webglcontextrestored`，自動重建模擬 |
| 字型 CDN 失效 | `font-display: swap`＋系統字型 fallback，無感降級 |

## 9. 觸控與捲動

- 畫布元素 `touch-action: none`，互動時不觸發頁面捲動
- 畫布不滿版寬（兩側留邊），手機用戶可從畫布外捲動頁面

## 10. 驗收清單（完成定義）

- [ ] 滴墨：點擊出現墨滴並自然暈開、與鄰色交融
- [ ] 吹墨：拖曳推出觸鬚狀紋路
- [ ] 傾斜：拖曳後整片墨往該方向流動、漸停
- [ ] 減法混色：墨色沉入米色紙底（非發光疊加），兩滴墨交疊處顏色更深
- [ ] 紙質感：畫布有細微纖維紋理與淡 vignette，不是平滑純色
- [ ] 初始滴墨：載入後自動錯時滴三滴
- [ ] 自動演出：閒置約 6 秒後自動滴墨與輕水流，一互動即停
- [ ] 色盤：切換後新墨採用新色，色球顯示色號名
- [ ] 清除鍵：墨於約 1.5 秒內漸淡被洗走（非瞬間清空）
- [ ] 產品卡：四色卡渲染自 `INKS`，按鈕連結可點（佔位 `#`）
- [ ] 作品區：磚牆排列，佔位視覺呈現
- [ ] 手機模擬：觸控作畫順暢、畫布外可捲動頁面、無明顯卡頓
- [ ] WebGL 停用時：fallback 視覺與文案正常顯示
- [ ] Console 零錯誤
- [ ] 決策者親手驗收通過

## 11. 佔位內容清單（日後由決策者替換）

| 項目 | 現值 |
|------|------|
| 購買連結 | `#`（全部） |
| 色號名與色值 | 初潮／夜枝／鎏金／靜淵（示意） |
| 標語 | 「讓墨自己說話」（示意） |
| 作品照 | 程式生成佔位視覺 |
| 品牌故事文案 | 佔位文案 |
| 社群連結 | `#` |

## 12. 範圍外（明確不做）

- 購物車、金流、訂單系統
- 後端、CMS、會員
- 多語系
- 部署與網域設定（檔案可直接丟靜態空間，本期不含）
- Canvas 2D 完整備援引擎
- 音效
